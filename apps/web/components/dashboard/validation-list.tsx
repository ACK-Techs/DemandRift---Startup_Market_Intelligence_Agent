import { ArrowUpRight } from "lucide-react";
import { ConfidenceBadge } from "@/components/ui/confidence-badge";
import { DecisionBadge } from "@/components/ui/decision-badge";
import { recentValidations } from "@/lib/mock-data/dashboard";

export function ValidationList() {
  return <section className="rounded-xl border border-[var(--line)] bg-white"><div className="flex items-center justify-between border-b border-[var(--line)] px-5 py-4 sm:px-6"><div><h2 className="text-sm font-semibold tracking-[-0.02em] text-[var(--ink)]">Recent validations</h2><p className="mt-0.5 text-[11px] text-[#858690]">Completed decisions and their evidence confidence</p></div><button className="text-xs font-semibold text-[var(--brand-deep)] transition hover:text-[var(--brand)]" type="button">View all</button></div><div>{recentValidations.map((validation) => <button className="group flex w-full items-center gap-3 border-b border-[var(--line)] px-5 py-4 text-left transition last:border-b-0 hover:bg-[#fcfcfd] sm:px-6" key={validation.name} type="button"><span className="grid h-9 w-9 shrink-0 place-items-center rounded-lg bg-[#f2f2f6] text-xs font-semibold text-[#53545e]">{validation.name.split(" ").slice(0, 2).map((part) => part[0]).join("")}</span><span className="min-w-0 flex-1"><span className="block truncate text-xs font-semibold text-[var(--ink)]">{validation.name}</span><span className="mt-0.5 block truncate text-[11px] text-[#858690]">{validation.segment}</span></span><span className="hidden shrink-0 sm:block"><DecisionBadge decision={validation.decision} /></span><span className="hidden w-[92px] shrink-0 text-right md:block"><ConfidenceBadge value={validation.confidence} /><span className="mt-1 block text-[10px] text-[#92939b]">{validation.updatedAt}</span></span><ArrowUpRight aria-hidden="true" className="h-4 w-4 shrink-0 text-[#a1a2aa] transition-transform duration-150 group-hover:translate-x-0.5 group-hover:-translate-y-0.5 group-hover:text-[var(--brand)]" /></button>)}</div></section>;
}
