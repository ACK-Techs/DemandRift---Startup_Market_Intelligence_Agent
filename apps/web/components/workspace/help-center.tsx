"use client";

import { ChevronDown, Search, ShieldCheck } from "lucide-react";
import { useMemo, useState } from "react";

const articles = [["How research works", "Understand the path from idea to decision.", "Start with a focused brief, review the research plan and trace conclusions back to retained evidence."], ["Evidence & citations", "Learn what makes a source traceable and useful.", "Every retained insight keeps its source context, capture date and relevance signal."], ["Decision confidence", "See why confidence is never a substitute for evidence.", "Confidence combines demand, severity and evidence quality so the recommendation stays explainable."], ["Account & workspace", "Manage your project space and preferences.", "Use Settings to update workspace preferences and the top bar toggle to change themes."], ["Contact support", "Get help with your DemandRift workspace.", "Share the relevant project and a short description of the issue with the support team."], ["Product updates", "Read what is new in the platform.", "Updates will appear here as the research workspace evolves."]] as const;
function Card({ children, className = "" }: { children: React.ReactNode; className?: string }) { return <section className={`rounded-xl border border-[var(--line)] bg-white ${className}`}>{children}</section>; }
export function HelpCenter() { const [query, setQuery] = useState(""); const [open, setOpen] = useState<string | null>(articles[0][0]); const filtered = useMemo(() => articles.filter(([title, detail]) => `${title} ${detail}`.toLowerCase().includes(query.toLowerCase())), [query]); return <div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_300px]"><div className="space-y-4"><Card className="p-5"><label className="relative block"><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-[#8d8e98]" /><input className="w-full rounded-lg border border-[var(--line)] bg-transparent py-3 pl-9 pr-3 text-sm outline-none focus:border-[var(--brand)] focus:ring-4 focus:ring-[var(--brand-soft)]" onChange={(event) => setQuery(event.target.value)} placeholder="Search help articles" value={query} /></label></Card>{filtered.map(([title, detail, content]) => <Card className="overflow-hidden" key={title}><button aria-expanded={open === title} className="flex w-full items-start justify-between gap-4 p-5 text-left" onClick={() => setOpen(open === title ? null : title)} type="button"><span><span className="block text-sm font-semibold text-[var(--ink)]">{title}</span><span className="mt-1.5 block text-xs leading-5 text-[#73747e]">{detail}</span></span><ChevronDown className={`mt-1 h-4 w-4 shrink-0 text-[#777883] transition ${open === title ? "rotate-180" : ""}`} /></button>{open === title && <p className="border-t border-[var(--line)] px-5 py-4 text-xs leading-5 text-[#686973]">{content}</p>}</Card>)}{filtered.length === 0 && <Card className="p-10 text-center text-sm text-[#777883]">No help articles match this search.</Card>}</div><Card className="h-fit p-5"><ShieldCheck className="h-5 w-5 text-[var(--brand)]" /><h2 className="mt-4 text-sm font-semibold">Need a hand?</h2><p className="mt-2 text-xs leading-5 text-[#73747e]">Our support team can help you understand a research run or the evidence in a report.</p><button className="mt-5 w-full rounded-lg bg-[var(--brand)] px-3 py-2.5 text-xs font-semibold text-white transition hover:bg-[var(--brand-deep)]" type="button">Contact support</button></Card></div>; }
